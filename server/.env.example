PORT=8787

# Required — Atlas won't respond without this
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

# Optional — enables ElevenLabs TTS (Jarvis-quality British voice).
# Without these, Atlas falls back to the browser's built-in speech synthesis.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
